package com.keola.ecommerce_backend.config;


import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api-keola")
public class BaseController {
    // Esta clase puede estar vacía, su única función es proporcionar el prefijo común
}